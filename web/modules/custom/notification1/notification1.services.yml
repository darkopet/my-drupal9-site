services:
  # Name of this service.
  notification.list:
    # Get Notifications
    class: '\Drupal\notification1\GetNotificationList'
    arguments: [ '@entity_type.manager', '@token', '@request_stack', '@current_user' ]
