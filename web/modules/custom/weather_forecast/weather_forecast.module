<?php

/**
 * Implements hook_theme().
 */
function weather_forecast_theme($existing, $type, $theme, $path): array {
  return [
    'weather_forecast' => [
      'variables' => [
        'data' => [],
      ],
    ],
  ];
}
